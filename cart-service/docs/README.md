# Cart Service

Redis-backed shopping-cart service.

Base path: `/api/v1/cart`. Authenticated users manage only their own cart. The cart stores product IDs and quantities; it is not a price authority.
