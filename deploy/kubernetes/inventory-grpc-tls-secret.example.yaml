# Do not commit real certificates. Your secret manager/controller must render this secret.
apiVersion: v1
kind: Secret
metadata:
  name: inventory-grpc-tls
  namespace: ecommerce
type: kubernetes.io/tls
stringData:
  tls.crt: REPLACE_WITH_SERVER_CERTIFICATE
  tls.key: REPLACE_WITH_SERVER_PRIVATE_KEY
  ca.crt: REPLACE_WITH_TRUSTED_CLIENT_CA
