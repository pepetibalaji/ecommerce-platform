# Config Server

Spring Cloud Config Server supplying externalized service configuration.

Services use `CONFIG_SERVER_URL` and can use an optional import locally. Production credentials and secrets remain deployment-owned.
