# Low-Level Design

`SecurityConfig`, `GatewayRateLimitConfig`, and `GatewayFallbackController` provide security, request protection, and dependency-error handling. Routes come from external configuration.
